# Configuration du serveur
PORT=3000

# Clé secrète pour les sessions (à changer en production)
SESSION_SECRET=change_this_secret_key_in_production

# Mode debug (true = messages d'erreur détaillés, false = production)
DEBUG=true
